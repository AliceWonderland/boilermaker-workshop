import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
<div>Hello world! BY DANNI AND ALICE</div>,
    document.getElementById('app') // make sure this is the same as the id of the div in your index.html
);